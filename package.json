{
  "name": "abogados-consultores",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node scripts/generar-json.js",

    "build": "npm run clean && npm run build:css && npm run build:js && npm run build:html && npm run build:assets",

    "clean": "rimraf dist",

    "build:css": "postcss css/style.css -o dist/style.css",
    "build:js": "esbuild scripts/main.js --bundle --minify --outfile=dist/main.min.js",

    "build:html": "cpx index.html dist && cpx articulo.html dist",

    "build:assets": "cpx index.json dist && cpx \"articulos/**/*\" dist/articulos && cpx \"images/**/*\" dist/images && cpx \"documentos/**/*\" dist/documentos"
  },
  "devDependencies": {
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "esbuild": "^0.19.0",
    "cpx": "^1.5.0",
    "rimraf": "^5.0.0"
  }
}





















